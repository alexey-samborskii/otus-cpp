server:
  protocol: http
  host: 0.0.0.0
  port: 8080
  threads: 4
  public_dir: public

storage:
  database: data/tasks.db

tls:
  certificate_file: certs/server.crt
  private_key_file: certs/server.key

logging:
  level: info
